/**
 * @file main.c
 * @brief RTOS 测试入口
 *
 * 主函数调用测试框架启动器。
 * 所有测试代码已移至 src/tests/ 目录，通过模块注册机制自动收集。
 */

#include "test_framework.h"

/*============================================================================
 * 主函数
 *============================================================================*/

int main(void) {
    /* 启动测试运行器（初始化硬件、内核，运行所有测试） */
    test_runner_start();

    /* 不应该到达这里 */
    while (1);
    return 0;
}
