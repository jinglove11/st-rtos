# My-RTOS 全面微内核化审查与实施计划

> 文档状态：初版审查基线  
> 审查日期：2026-07-10  
> 源码基线：`4dfe07d fix(smp): 回退 PendSV spin_lock — 死锁根因`  
> 主平台：RP2350 / Raspberry Pi Pico 2 W / Cortex-M33 Non-secure  
> 兼容平台：STM32F767 / Cortex-M7  
> 本文目标：在保留已有能力、IPC、MPU、用户态服务和测试资产的基础上，把项目从“具备微内核机制的 RTOS 原型”推进为“边界清晰、最小权限、可恢复、可验证、可裁剪的嵌入式微内核操作系统”。

---

## 1. 结论先行

My-RTOS 已经不是普通的最小 RTOS。项目已经具备任务隔离、SVC 系统调用、用户指针校验、能力句柄、endpoint/channel IPC、一次性 reply cap、MPU 显式映射、用户态 name server、用户态 FS/driver server 原型、fault endpoint、supervisor、SMP 和板上测试框架。这些都是继续微内核化的有效基础，不应推倒重写。

但按“真正微内核”的标准，当前仍属于：

**具备较多微内核机制的实验型 RTOS，而不是已经完成边界收敛的微内核操作系统。**

主要原因不是缺少功能数量，而是以下四个基础条件还没有同时成立：

1. 构建配置没有唯一可信来源，配置开关与实际参与编译的头文件存在分叉。
2. 双核已启用，但共享调度队列、能力池、IPC 对象和统计数据没有形成完整的跨核同步协议。
3. 用户态资源创建仍存在越权路径，例如任意用户任务可申请任意外设 MMIO、订阅 fault endpoint、擦写文件系统 Flash 区。
4. 系统正常启动路径仍以特权测试框架和特权 shell 为中心，root/init 尚未成为唯一系统编排者。

### 1.1 当前成熟度判断

成熟度不是测试通过率。测试通过说明已覆盖路径在当前环境可工作，不等于安全边界、并发模型和故障模型已经闭合。

| 维度 | 当前判断 | 说明 |
| --- | ---: | --- |
| 微内核基础机制 | 65% | syscall、cap、IPC、MPU、fault、reply cap 已有实质实现 |
| 微内核边界收敛 | 40% | shell、timer/BH 回调、资源创建策略仍有较多特权路径 |
| 单核运行稳定性 | 75% | 板上已有大规模回归记录，但仍需冷启动、长稳和故障注入门禁 |
| 双核正确性 | 25% | 能启动双核，但关键共享结构仍有已知数据竞争 |
| 用户态系统服务 | 45% | name server、FS、driver、supervisor 已有原型，编排和权限仍未闭合 |
| 能力安全模型 | 50% | derive/copy/move/revoke/reply 已有，但 CSpace、对象生命周期和策略授权不足 |
| 内存/地址域隔离 | 45% | MPU 隔离有效，但映射槽固定、进程数据域和 loader 生命周期不完整 |
| 工程化与发布 | 40% | 有板上测试和脚本，但配置、双构建系统、测试/产品镜像仍未统一 |
| 完整嵌入式 OS 能力 | 30% | 持久 FS、网络、用户 libc、安全启动等仍不完整或仅有配置占位 |

综合判断：

- 若目标是“微内核核心架构成立”，当前约完成 **45%**。
- 若目标是“可长期运行、可发布的完整嵌入式微内核 OS”，当前约完成 **30%**。
- 达到可称为“真正微内核”的关键不是先补 Wi-Fi、GUI 或 TLS，而是先完成本文 M0-M7 的边界收敛。

---

## 2. 审查范围与方法

本次审查覆盖：

- 构建系统：`Makefile`、`CMakeLists.txt`、Kconfig、defconfig、生成配置头。
- 启动路径：`main`、test runner、`kern_init`、`kern_start`、root bootstrap、init。
- 调度与任务：TCB、ready queue、PendSV/SVC、阻塞/唤醒、SMP、任务回收。
- 能力系统：全局 cap pool、每任务 cap set、derive/copy/move/revoke、对象清理。
- IPC：endpoint、channel、reply cap、cap transfer、等待队列和 syscall continuation。
- 内存保护：PMSAv8/PMSAv7 MPU、用户栈、SHM、MMIO、usercopy、ELF loader。
- 故障与恢复：fault handler、fault endpoint、supervisor、panic log、watchdog/reset。
- 服务：name server、allocator、sync、driver registry、UART server、FS server、shell。
- 驱动与存储：UART、GPIO、Flash block、device registry、FS store。
- 诊断与测试：trace、stats、模块化板上测试、回归脚本和 CI 本地脚本。

审查基线的静态规模：

- `src` 下 C/H/S 合计约 49,356 行。
- 非测试主体代码约 28,518 行。
- 测试代码约 20,754 行，测试源文件 37 个。
- 静态检索到约 2,575 个测试断言/检查调用。
- 本次执行 `make -j4`，RP2350 CMake 构建通过。
- 真机串口运行不在本次自动审查环境内；项目此前已有 2900/2900 板上通过记录，应在后续每个阶段重新建立可重复的自动真机证据。

---

## 3. 目标定义：本项目中的“真正微内核”

RP2350 Cortex-M33 没有 MMU，不能复制 seL4/QNX/Fuchsia 的完整虚拟地址空间模型。因此本项目的目标应定义为“MPU 型嵌入式微内核”，而不是宣称具备 MMU 进程隔离。

### 3.1 内核最终只保留的机制

最终内核应只负责：

1. CPU/异常/上下文切换。
2. 线程调度、时间预算和跨核调度协调。
3. capability/CNode 和内核对象生命周期。
4. endpoint/reply/notification 等最小 IPC 原语。
5. MPU 地址域装载、用户复制和受控内存映射。
6. IRQ 控制对象、mask/ack、最小中断入口和通知投递。
7. 单调时钟、最小内核 timer 对象和超时队列。
8. fault 捕获、故障信息封装和投递。
9. 启动时创建第一个 root/init 用户任务所需的最小 bootstrap。

### 3.2 必须移到用户态的策略

以下策略不应长期留在内核或特权 shell：

- 服务发现和命名策略。
- 服务依赖图、启动顺序和重启策略。
- 设备枚举、驱动选择和 MMIO/IRQ 分配策略。
- 文件系统 inode、路径、fd、缓存和持久化策略。
- shell 命令、诊断展示和系统管理命令。
- 网络协议栈、socket 代理、DHCP/DNS/TLS。
- 用户堆分配策略和大块共享内存仲裁。
- 日志收集、trace 消费和 crash 报告策略。

### 3.3 完成定义

只有同时满足以下条件，项目才进入“微内核架构完成”状态：

- 除 idle、内核 IRQ/BH 最小执行器和必要的内核维护线程外，系统服务都以非特权任务运行。
- 用户态 API 不暴露裸 task id、endpoint id、IRQ 号作为授权凭据，只接受 capability slot。
- 用户任务不能凭参数自行获得 MMIO、IRQ、Flash、fault endpoint 或其他全局资源。
- 所有阻塞 syscall 都通过统一 continuation 返回，不在 SVC handler 中自旋或 WFI。
- 所有对象都有明确 owner、引用、撤销、删除、任务死亡和并发访问语义。
- 双核开启时，所有共享可变状态都有可证明的同步策略；单核与双核使用同一语义测试。
- root/init 是系统唯一初始权威，其他服务只能获得 root 显式授予的能力子集。
- 任一普通用户服务崩溃不会破坏内核、其他服务或其不持有能力的资源。
- 产品镜像不依赖测试框架启动，测试镜像和产品镜像使用同一内核但不同启动清单。

---

## 4. 当前架构盘点

### 4.1 已经可以保留并演进的基础

| 子系统 | 当前有效基础 | 后续方向 |
| --- | --- | --- |
| 任务/调度 | 32 优先级、抢占、时间片、join、fault cleanup、per-CPU current | 补齐 SMP 正确性、affinity、预算和统一 continuation |
| MPU | Flash RO+X、用户栈 RW+XN、Region 3..7 显式映射、PSPLIM | 引入用户 image/data domain 和映射对象管理 |
| usercopy | 按任务 MPU region 校验读写权限 | 统一长度溢出检查、ABI 结构版本和 fuzz |
| capability | slot+generation、rights、derive、copy/move、revoke tree、cleanup hook | 升级为 CSpace/CNode、对象 generation、原子事务 |
| endpoint | request/reply、超时、cap transfer、一次性 reply cap | 稳定 IPC ABI、notification、全阻塞 continuation |
| channel | 消息、cap transfer、SHM 接口 | 明确是否保留为内核原语或改为用户库封装 |
| fault | 用户 fault 隔离、寄存器诊断、fault endpoint、panic dump | 权限化订阅、服务身份、可靠恢复闭环 |
| root/init | root bootstrap 和用户 init 已存在 | 让它成为正常启动主路径和唯一资源分配根 |
| 用户服务 | name server、FS、driver、sync、supervisor 原型 | 去除特权旁路，统一服务清单与协议 ABI |
| 测试 | 模块化板上回归、资源快照、串口脚本 | 加 host 单测、随机/压力、复位/掉电和双核门禁 |

### 4.2 当前实际启动链

当前主入口是：

```text
main
  -> test_runner_start
      -> system_init
      -> 创建特权 test_runner
      -> kern_start
          -> timer_svc（特权）
          -> bh_svc（特权）
          -> 调度器
      -> test_runner 启动 core1
      -> 执行全部板上测试
      -> 创建用户 init
      -> 启动特权 shell
```

目标启动链应是：

```text
reset/boot
  -> arch + board early init
  -> microkernel init
  -> 创建 root/init 用户地址域和初始 CSpace
  -> root/init 读取静态 service manifest
      -> log/console server
      -> name server
      -> resource/allocator server
      -> supervisor
      -> driver manager + driver servers
      -> FS server
      -> shell/management server
      -> applications
  -> 内核只保留机制线程
```

测试镜像则由 root/init 启动 test manager，不再由 `main` 固定进入 test runner。

---

## 5. 代码审查发现

以下问题按严重程度排序。P0/P1 项是后续微内核化的前置阻断项。

### 5.1 P0：配置生成产物与编译输入分叉

证据：

- `scripts/menuconfig.py:528` 生成 `src/kernel/include/kernel_config.h`。
- `Makefile:445` 也把该文件声明为生成目标。
- CMake 的首个内核 include 路径却是 `include/kernel`。
- Make 的 CFLAGS 同样包含 `-Iinclude/kernel`。
- 仓库同时跟踪 `include/kernel/kernel_config.h` 和 `src/kernel/include/kernel_config.h`。
- 两者当前内容不一致，例如测试模块列表和 `VFS_ENABLE` 值不同。

影响：

- `menuconfig` 看似成功，实际编译配置可能未改变。
- 不同源文件或不同构建系统可能看到不同配置。
- 测试结果、功能矩阵和发布镜像不可复现。

处理原则：

- 全仓只允许一个生成配置头。
- 配置头必须输出到 build directory，而不是提交两个副本。
- CMake 和 Make 必须消费同一个 `.config` 和同一个生成器。
- CI 必须验证“修改配置后目标宏和二进制符号确实变化”。

### 5.2 P0：SMP 调度器存在源码已承认的数据竞争

证据：

- `src/kernel/core/scheduler.c:939-944` 明确说明 PendSV 不持 `sched_lock`，双核会并发修改共享 ready list。
- `src/kernel/core/scheduler.c:961`、`:968`、`:995` 在无跨核锁条件下修改双向链表和 bitmap。
- 两个核心各自启用 SysTick；`sched_tick_handler` 在两核都递增同一全局 tick。
- timeout scan、task reclaim、stats update 也可能由两个核心并发执行。
- `SMP_WORK_STEALING` 已配置开启，但源码没有 affinity/runqueue/work-stealing 实现。

影响：

- ready list 断链、同一任务被两个 CPU 同时运行、重复回收、超时重复唤醒。
- 系统时间按核心数加速，timeout 和 CPU usage 失真。
- 调试打印改变时序后问题消失，符合典型竞态特征。

处理原则：

- 在 per-CPU runqueue 完成前，默认关闭 SMP 产品配置。
- 不在 PendSV 里直接阻塞等待普通 spinlock。
- 采用“per-CPU runqueue + 明确任务归属 + IPI reschedule”方案。
- 只允许一个 timekeeper 更新全局单调时钟，另一个核只处理本地时间片。

### 5.3 P0：capability 的 SMP 锁覆盖不完整

证据：

- `cap_create_for`、`cap_delete`、`cap_revoke_for` 使用 `cap_pool_lock`。
- `cap_lookup_for`、`cap_derive_for`、`cap_copy_to`、`cap_move_to`、`cap_revoke_all`、`cap_revoke_object`、refcount/free-count 查询没有统一持锁或 RCU 协议。
- cap tree、owner CSpace 和对象 cleanup hook 会跨多个结构更新。

影响：

- derive/revoke、lookup/delete、transfer/task-exit 并发时可能出现 use-after-free、树断裂、重复 cleanup 或权限检查使用旧值。
- `CAP_RCU=1` 当前更多是配置声明，不是完整实现。

处理原则：

- 先实现单一 cap-table writer lock + 有界读锁，正确性优先。
- cleanup/revoke hook 不得在持 cap 全局锁时执行可能获取其他锁的操作。
- 后续有性能证据后再引入 epoch/RCU，不能以配置项代替实现。

### 5.4 P0：关键资源授权可被普通用户任务自行获取

证据：

- `sys_mmio_request` 注释明确说明任何用户任务都可请求 `0x40000000..0x60000000` 范围内任意 MMIO。
- `sys_flash_op` 没有 capability 参数，任意用户任务可读、擦除和写入 FS Flash 区。
- `sys_fault_subscribe` 可被任意用户任务调用，并授予 fault endpoint 的 `CAP_FULL`。
- 普通用户可直接 `sys_task_create`，并得到新任务的 `CAP_FULL` 管理能力。

影响：

- 一个被攻破的普通服务可以控制时钟、GPIO、UART、DMA 等外设寄存器。
- 可破坏持久数据、panic log 或其他 Flash 区策略数据。
- 可窃取全系统故障事件，向 fault endpoint 注入消息或消耗队列。

目标修复：

- 删除 `sys_mmio_request(base,size,width)` 的普通用户路径，改成 root 持有静态 platform resource caps，再定向派生给 driver server。
- `sys_flash_op` 改为 `block_cap + operation + range`，只允许 block server 获得。
- fault endpoint 只给 supervisor 一个只读 notification cap；timer 使用独立 endpoint 或 wait-set，不再要求 fault endpoint `CAP_FULL`。
- task create 改为 factory/task-control capability，普通任务没有 factory cap 就不能创建子任务。

### 5.5 P0：正常镜像仍固定依赖测试框架和特权 shell

证据：

- `src/app/main.c` 无条件调用 `test_runner_start()`。
- `src/tests/test_framework.c` 没有整体 `TEST_ENABLE` 外层保护。
- CMake 无条件 glob 全部 `src/tests/*.c`。
- `shell_start` 使用 `task_create`，shell 是特权任务。
- shell 直接调用 `endpoint_create`、`task_create_user`、`cap_create_for`、`task_delete` 等内核 C API，并直接修改新任务初始栈帧的 R0。

影响：

- 产品启动链与测试启动链没有真正分离。
- shell 成为事实上的全权 root，绕过 syscall/capability 安全模型。
- 服务能否在纯用户态被 init 正确启动没有成为唯一真相。

目标修复：

- 建立 `IMAGE_PROFILE=test|dev|release`。
- `main` 只负责 early boot 和启动 root/init。
- test manager、shell、service manager 都走用户态 IPC 和能力。
- 禁止应用层 include 内核私有 API；用构建规则检查。

### 5.6 P1：普通 IPC 对象使用关本核中断，不等于双核互斥

证据：

- endpoint、channel、semaphore、mutex、mqueue、event 大量使用 `hal_enter_critical()`。
- 该操作只屏蔽当前 CPU 中断，不能阻止另一核心修改相同对象。
- endpoint 的 pool bitmap、消息环、wait queue、reply binding 都是全局共享结构。

影响：

- 双核客户端/服务端并发时会破坏 queue、count 和 reply 绑定。
- 目前 SMP 测试通过不能证明跨核高并发正确性。

目标修复：

- 每个 IPC 对象增加短临界区 spinlock，或按 CPU 分片。
- 明确锁顺序：scheduler -> object -> cap，禁止反向嵌套。
- wait queue 入队与任务状态切换必须是一个原子事务。
- 建立跨核随机 send/recv/delete/timeout/fault 压力测试。

### 5.7 P1：task capability 只保存 task id，存在 ID 复用陈旧授权风险

证据：

- task cap 的 object 使用 `(void *)(uintptr_t)(id + 1)`。
- 任务退出只 `cap_revoke_all(dying_task_id)`，这只撤销“由该任务拥有”的能力。
- 其他任务持有的、指向该 task id 的管理 cap 不会因目标任务死亡自动全部失效。
- TCB 回收后 task id 可复用。

影响：

- 旧 task cap 可能在 id 复用后控制一个无关的新任务。

目标修复：

- task cap 必须指向稳定的 task object，包含独立 generation/lifecycle state。
- 目标任务终止时调用 `cap_revoke_object(task_obj, CAP_OBJ_TASK)`。
- 所有内核对象都应用同样的对象 generation 和销毁协议，不能仅依赖池索引。

### 5.8 P1：阻塞 syscall continuation 尚未统一

证据：

- syscall 文件注释仍说明部分阻塞 IPC 在 SVC handler 中不安全。
- task TCB 只有通用 `syscall_blocked` 和少量 subsystem side tables。
- 内核线程 API 中 semaphore/mutex/mqueue/event/channel/endpoint 仍存在等待状态下 `WFI` 的同步 C 调用路径。

影响：

- 同一 IPC 原语存在 kernel-thread 同步接口和 user-syscall continuation 两套状态机。
- 删除、超时、fault、cap transfer rollback 的组合容易出现遗漏。

目标修复：

- 引入统一 `syscall_continuation_t`，保存 syscall 号、阶段、用户输出地址、对象 generation、超时和 rollback 数据。
- 每个阻塞 syscall 首次调用只入队并返回 `KERN_SYSCALL_BLOCKED`。
- 唤醒路径只写 continuation 结果并使任务 ready；恢复到线程态后返回。
- 删除 SVC handler 内的等待循环和隐式 WFI。

### 5.9 P1：用户态服务仍有特权旁路和全局 SRAM 状态

证据：

- allocator 文件名为 user-space server，但注释要求用 `task_create` 作为特权任务运行。
- `fs_runtime.c`、`driver_runtime.c` 和 supervisor 的旧 registry 使用可写静态全局变量。
- 真正用户任务默认不能访问全局 SRAM，这些模块只能由特权 shell 使用或必须重构状态存储。
- FS store 已经采用“显式 mem cap 映射 + context 参数”，这是应推广的正确模式。

目标修复：

- 所有用户服务状态统一放入其私有 data region/heap，不再依赖内核全局 BSS。
- allocator 改为普通用户任务，root 只授予内存工厂 capability。
- runtime client 状态改为显式 context，不使用进程外共享 singleton。

### 5.10 P1：ELF loader 仍是实验实现

证据：

- `elf_load` API 没有 image length 参数，program header 和 segment offset 无法完整做边界校验。
- 只处理一个 writable PT_LOAD。
- loader 注释明确承认 data RAM 未跟踪，任务退出不回收。
- 可执行区仍映射整个 board Flash，而不是进程自身代码区。

目标修复：

- loader 输入必须是 `(image, length, source_cap)`。
- 校验所有加法溢出、header/segment 边界、对齐、W^X 和 entry 所属 executable segment。
- image object 保存 code/data/stack/relocation 和 cleanup 列表。
- task 退出时原子撤销映射并回收所有 backing object。

### 5.11 P2：配置表包含大量“已开启但未实现”的功能

当前生成头打开了 CYW43、NET、DHCP、DNS、mbedTLS、LittleFS、OTA、Secure Boot、PM、动态链接、GUI 等选项；Kconfig 自己又声明这些是尚未实现的 placeholder。CMake 构建警告也显示 CYW43/lwIP 依赖未初始化。

目标规则：

- `CONFIG_X=y` 必须满足：源码已链接、启动路径可达、至少一个自动测试、文档说明限制。
- 未达到者只能命名为 `EXPERIMENTAL_X`，并默认关闭。
- full preset 可以打开实验功能，但 CI 必须清楚标记“不代表可用”。

### 5.12 P2：双构建系统已经漂移

证据：

- RP2350 CMake 使用递归 glob 获取现有内核源码。
- STM32 Make 源列表仍引用已删除的 `src/kernel/vfs/inode.c`、`vfs.c`、`devfs.c`、`ramfs.c`。
- 两条路径使用的配置生成依赖也不同。

目标修复：

- 建立单一 manifest（CMake source list 或生成清单）。
- 两个平台共享内核 source target，board/arch 只替换底层实现。
- 每次提交至少 build RP2350 test/release 和 STM32 compile-only。

### 5.13 P2：诊断数据在 SMP 下也缺同步

- trace ring 的 head/count 和 entry 写入没有跨核同步。
- stats 全局计数和 per-task 统计没有原子或 per-CPU 分片。
- `stats_record_syscall` 当前递增 `ctx_switch_count`，语义混淆。

目标修复：

- trace 使用 per-CPU ring，读取端合并。
- stats 使用 per-CPU counter，查询时汇总。
- syscall count、context switch count、CPU tick 分开维护。

### 5.14 P2：部分协议和注释已落后于实现

例如 allocator 注释仍描述 IPC MOVE 会撤销新 child，但 `ipc_transfer.c` 已改为删除源 slot、保留目标 child。过期注释会误导后续设计。

目标修复：

- 每个阶段完成时同步删除“future/Phase”旧注释。
- 协议文档由 ABI 头生成字段表或至少由测试验证版本号和结构大小。

---

## 6. 目标系统架构

### 6.1 内核对象模型

所有对象统一使用以下生命周期：

```text
UNALLOCATED
  -> ALLOCATED（对象 generation 创建）
  -> LIVE（至少一个 capability 可达）
  -> DYING（禁止新操作，唤醒等待者）
  -> REVOKING（撤销全部派生 capability 和映射）
  -> DEAD（等待并发引用归零）
  -> UNALLOCATED（slot 可复用，generation 增加）
```

统一对象头建议：

```c
typedef struct kobject_header {
    uint16_t type;
    uint16_t generation;
    uint32_t refs;
    uint32_t flags;
    spinlock_t lock;
} kobject_header_t;
```

task、endpoint、reply、notification、IRQ、timer、frame/SHM、MMIO、factory、CNode 都必须服从同一对象协议。

### 6.2 Capability/CSpace

目标不是无限复制 seL4，而是实现适合 MCU 的有界 CSpace：

- 每任务一个根 CNode。
- CNode 由固定大小 leaf page 组成，例如 32 或 64 slot。
- cap handle 编码为 `cnode index + slot + generation`，不暴露全局 cap pool slot。
- 操作至少包括 `copy`、`mint/attenuate`、`move`、`delete`、`revoke`。
- rights 与 badge/guard 分离；mint 只能衰减权限，不能放大。
- 所有多 cap transfer 使用 prepare/commit/rollback 三阶段事务。
- root/init 初始持有 factory、platform resources、IRQ control、memory pool、system control。

### 6.3 IPC 原语

建议收敛到三类：

1. `endpoint_call/recv/reply`：同步 RPC，支持一次性 reply cap。
2. `notification_wait/signal`：异步位图/计数通知，用于 IRQ、timer、fault、service death。
3. `frame/SHM`：大数据传递，IPC 消息只传 offset/length 和 frame cap。

现有 channel 可选择：

- 保留为内核异步消息原语，但必须有明确独立价值；或
- 迁移为用户库中“endpoint + SHM ring”的高性能通道，减少内核表面积。

### 6.4 MPU 地址域

RP2350 只有 8 个 MPU region，建议固定预算：

| Region | 用途 |
| --- | --- |
| 0 | 当前进程代码/只读数据，而不是整片 Flash |
| 1 | 当前进程私有 data/bss/heap |
| 2 | 当前线程栈 + PSPLIM |
| 3 | IPC shared frame 0 |
| 4 | IPC shared frame 1 |
| 5 | service-specific mapping / MMIO 0 |
| 6 | MMIO 1 / DMA buffer |
| 7 | 临时 grant window 或保留 |

映射不足时返回明确的 `REGION_EXHAUSTED`，由用户态 runtime 做窗口换入换出。禁止重新映射全 SRAM。

### 6.5 服务拓扑

```text
root/init
├── supervisor
├── name-server
├── resource-manager
│   ├── memory/frame factory
│   ├── IRQ allocator
│   └── platform MMIO registry
├── driver-manager
│   ├── uart-server
│   ├── gpio-server
│   ├── flash/block-server
│   └── future i2c/spi/cyw43
├── fs-server
├── log-server
├── shell-server
└── applications
```

服务之间没有隐式全局权限，只通过 manifest 中的 capability grant 连接。

---

## 7. 分阶段实施路线

工期按一名熟悉项目的全职开发者估算，包含实现、测试、真机回归和文档。多人并行只能在依赖边界清楚后缩短日历时间。

## M0：建立可信基线（1-2 周）

目标：任何后续改动都基于可复现配置、构建和测试证据。

任务：

1. 删除双配置头设计，只保留 build directory 生成的 `kernel_config.h`。
2. CMake/Make 都显式依赖 `.config -> generated header`。
3. 用 `config.h` 中的 hash 或 build-id 打印当前配置摘要。
4. 建立 `test/dev/release` 三种镜像 profile。
5. test profile 才编译 `src/tests` 和 test runner。
6. release profile 从 `main` 启动 root/init，不运行测试。
7. 移除 CMake `GLOB_RECURSE`，使用显式 source manifest。
8. 修复 STM32 源列表中已删除 VFS 文件引用。
9. 所有 placeholder 配置默认关闭，并增加 `EXPERIMENTAL` 标识。
10. 固化当前 RP2350 真机测试总数、ELF hash、配置 hash 和串口日志。

验收：

- 修改任意 Kconfig 值后，两个平台实际预处理宏都同步变化。
- test/dev/release 三个镜像有不同且可解释的符号表。
- RP2350 build + flash + serial 自动通过。
- 连续执行 20 次 shell reset 和 20 次冷上电，全部到达测试或 root/init ready 标志。
- STM32 至少 compile/link 通过。

禁止进入下一阶段的条件：配置头仍有两个、测试镜像仍是唯一启动入口、SMP 默认仍开但无稳定门禁。

## M1：SMP 正确性优先（3-5 周）

目标：双核从“能运行”变成“共享状态有定义”。

任务：

1. 在 M1 完成前，release 默认 `SMP=n`，保留 SMP 专用实验 profile。
2. TCB 增加 `cpu_owner`、`affinity_mask`、`migration_state`。
3. 每 CPU 一个 runqueue、ready bitmap、idle task、need_resched 和调度统计。
4. PendSV 只操作本 CPU runqueue，不获取可能由被抢占线程持有的普通锁。
5. 跨核 wakeup 通过 IPI/FIFO 发 reschedule 请求。
6. 迁移协议采用 `RUNNING -> MIGRATING -> READY_REMOTE`，禁止同一 TCB 同时出现在两个队列。
7. Core 0 作为 timekeeper；Core 1 SysTick 只处理本地 slice，或禁用 Core 1 SysTick 改用共享 alarm。
8. task pool、cap pool、IPC pool 使用真正跨核锁。
9. endpoint/channel/sem/mutex/event/mqueue 建立对象锁。
10. trace/stats 改成 per-CPU。
11. 形成全局锁顺序文档和 lockdep 调试模式。

关键不变量：

- 任意时刻每个 live task 只能处于一个 CPU current 或一个队列。
- running task 不得出现在 ready/wait queue。
- 一个 object slot 只能被一个 generation 使用。
- 任何 wakeup 最多完成一次。
- tick 在相同物理时间内与 CPU 数无关。

测试：

- 两核各 100 万次 yield/wakeup。
- 跨核 endpoint ping-pong 100 万次。
- send/delete/timeout/fault 四事件随机交错。
- cap derive/revoke/lookup 并发压力。
- task create/exit/id reuse 并发压力。
- 30 分钟、8 小时、24 小时 soak。

验收：

- debug/release、带打印/不带打印行为一致。
- ready queue invariant 零失败。
- 测试期间没有同 TCB 双核运行记录。
- tick 漂移在硬件时钟允许范围内。

## M2：统一内核对象和 CSpace（4-6 周）

目标：解决陈旧句柄、对象复用和固定 32 槽限制。

任务：

1. 引入统一 `kobject_header_t` 和 object generation。
2. task cap 不再保存裸 id，改为 task object pointer + generation。
3. endpoint/channel/timer/IRQ/frame 等逐个迁移。
4. 建立 CNode leaf，先支持 64-128 slot/任务，可按配置扩展。
5. cap lookup 从全局 owner 扫描改为当前任务 CSpace slot 查找。
6. 引入 `CAP_OBJ_CNODE`、`CAP_OBJ_FACTORY`、`CAP_OBJ_FRAME`、`CAP_OBJ_SYSTEM`。
7. 定义 mint：rights 衰减 + badge 设置，不创建新内核对象。
8. copy/move/revoke 全部在事务 API 下实现。
9. cleanup hook 改为锁外延迟执行队列。
10. 任务死亡撤销目标 task object 的所有管理 cap。
11. 对 generation wrap 制定策略：扩大位宽或永久退休 slot。

验收：

- 旧 task cap 在 task id 复用后必须返回 stale-cap 错误。
- CSpace 满时 cap transfer 原子失败，源和目标均无半提交状态。
- revoke 大型派生树后对象只 cleanup 一次。
- 非 root 任务不能凭裸 id 访问任何对象。

## M3：稳定 syscall 与 IPC ABI（4-6 周）

目标：把 syscall 从大量散立接口收敛为稳定、可版本化的最小 ABI。

任务：

1. 定义 ABI 版本、错误码稳定性、32/64 位字段和结构对齐规则。
2. 所有 syscall 参数结构都包含 `size` 和 `version`。
3. 引入统一 continuation 状态机。
4. 完成 endpoint call/recv/reply 的 continuation。
5. 完成 cap-bearing IPC 的阻塞、超时和 rollback。
6. 增加 notification 对象，替换 fault/timer/IRQ 共用普通消息 endpoint 的做法。
7. reply cap 保持单次使用，并覆盖 client timeout、server death、endpoint delete、cap revoke。
8. 评估移除内核 sem/mutex/mqueue/event 的用户 syscall，只保留内核内部或兼容层。
9. channel 若保留，必须复用统一 continuation 和 transfer transaction。
10. 增加 syscall fuzz harness，随机生成无效 cap、pointer、length 和状态组合。

验收：

- SVC handler 内无等待循环/WFI。
- 所有阻塞 API 都能在 timeout、对象删除、任务 fault 后正确返回。
- 一次性 reply cap 第二次使用稳定失败。
- ABI 兼容测试可加载前一版本用户应用并得到明确兼容或拒绝结果。

## M4：地址域、用户内存和进程运行时（4-6 周）

目标：让普通用户程序拥有完整、可回收的私有数据域，而不访问全局 SRAM。

任务：

1. 定义 `address_space_t`/`mpu_domain_t`，从 TCB 中分离 mapping policy。
2. 为每个 process 分配 code、rodata、data/bss/heap、thread stack region。
3. 支持一个 process 多线程共享 data domain；每线程只切换 stack region。
4. frame allocator 只通过 factory cap 创建。
5. SHM/MMIO map 统一为 mapping object，并记录 owner、rights、region、lifetime。
6. loader API 增加 image length 和 source cap。
7. 完整校验 ELF headers、segments、entry、W^X 和 overflow。
8. loader 跟踪所有 backing，任务/进程退出时全部回收。
9. 用户 libc 提供 string、printf、malloc/free、errno、IPC stubs；不依赖内核全局 libc 状态。
10. 增加 stack/heap guard 和映射槽耗尽策略。

验收：

- 用户程序可使用全局/静态变量，且只能访问自己的 data domain。
- 两个进程的同名全局变量互不可见。
- ELF 重复加载/退出 10,000 次无内存和 cap 泄漏。
- W+X 映射被拒绝。

## M5：root/init 与最小权限启动（3-5 周）

目标：root/init 成为唯一初始权威，移除 shell 和测试框架的内核旁路。

任务：

1. 设计静态 service manifest，描述 entry/image、priority、stack、CPU affinity、restart policy、依赖和初始 caps。
2. root bootstrap 只创建 root task、root CNode、factory、platform resource caps 和 root endpoint。
3. init 通过 capability API 创建所有服务。
4. shell 改为普通用户服务，只获得 management RPC cap，不获得内核 C API。
5. test manager 改为用户服务，按 manifest 仅在 test profile 启动。
6. 禁止 `src/user`、`src/app` include 内核私有头；CI 做 include boundary 检查。
7. 删除 shell 直接修改 TCB/初始栈帧的逻辑。
8. 为 system shutdown/reset 建立 `CAP_OBJ_SYSTEM`，只有 init/management service 持有。

验收：

- release 镜像中没有 test runner 和特权 shell。
- root 之外的普通任务没有 factory/system/platform root cap。
- 从启动日志可导出完整 capability grant 图。

## M6：服务框架与监督树（4-6 周）

目标：形成稳定的用户态服务运行环境。

任务：

1. 统一 service protocol header：magic、ABI version、opcode、flags、request id、payload length、status。
2. name server 使用 mint badge 标识注册者，不能信任客户端自报 owner badge。
3. allocator 改为非特权 resource server，持有 memory factory cap。
4. sync server 使用延迟 reply/notification 队列，不再轮询 trylock。
5. supervisor 管理服务实例 ID，而不是只按 task name 匹配。
6. fault 事件包含 task object generation、service instance、fault class 和 crash sequence。
7. 支持 restart budget、指数退避、依赖级联、circuit breaker 和永久 quarantine。
8. supervisor 自身由 init 监控；init 故障策略明确为受控 reboot。
9. 服务状态和配方放私有映射内存，不依赖全局 BSS。
10. 服务重启时重新派生最小 cap 集，旧实例所有 cap 必须撤销。

验收：

- 任意服务连续崩溃不会耗尽 task/cap/heap。
- 重启实例不能使用旧实例遗留 cap。
- nameserver、FS、driver、shell 任一崩溃后按策略恢复，其他服务继续工作。

## M7：驱动真正用户态化（6-10 周）

目标：内核不再包含设备策略，只提供 MMIO/IRQ/frame 机制。

顺序：

1. UART console server。
2. GPIO server。
3. Flash/block server。
4. I2C/SPI server。
5. DMA buffer 和 IRQ ack 协议。
6. CYW43 放在更后阶段。

每个 driver server 必须：

- 由 root 按静态白名单授予精确 MMIO range 和 IRQ cap。
- 无权请求其他外设范围。
- IRQ 到达后内核 mask，发送 notification；driver 处理后显式 ack/unmask。
- 大数据使用 frame/SHM，不把 payload 固定在几十字节消息内。
- 崩溃后 IRQ 保持 masked，MMIO mapping 撤销，等待 supervisor 重启。

内核侧需要删除或限制：

- 普通用户 `sys_mmio_request`。
- 用户可直接注册函数指针 IRQ/BH/timer callback 的接口。
- device registry 中的命名、open count 和设备策略。

验收：

- UART/GPIO/Flash driver 都在 nPRIV=1 下工作。
- 驱动越界 MMIO 访问触发用户 fault，不影响内核。
- driver crash 后没有未确认 IRQ 风暴。
- 未持设备 cap 的任务无法操作设备。

达到 M7 后，项目可首次称为“微内核架构基本成立”。

## M8：文件系统与持久存储（5-8 周）

目标：所有路径、inode、fd 和持久化逻辑位于用户态 FS server。

任务：

1. 保留当前 context-based FS store，补齐并发和 per-client session。
2. fd 改为服务端 generation handle，防止复用陈旧 fd。
3. 用 client badge/endpoint identity 识别所有者，不依赖裸 task id。
4. block server 通过 block cap 暴露分区，不暴露全 Flash。
5. 引入 littlefs 或同类掉电安全 FS；第三方库作为用户服务依赖。
6. 增加 mount namespace、devfs 代理、目录迭代和权限模型。
7. 所有 Flash erase/program 经过 block server capability。
8. panic log 使用内核保留分区或启动早期受控写接口，与普通 FS partition 分离。

验收：

- 随机掉电注入后 FS 可恢复或明确回滚。
- 客户端 crash 自动关闭其 fd，不影响其他客户端。
- FS server crash/restart 后持久文件仍可读，旧 fd 明确失效。

## M9：可靠性、实时性和诊断（4-6 周）

目标：把“测试通过”升级为“长期运行行为有证据”。

任务：

1. watchdog 由 root/supervisor 按健康协议喂狗，普通任务无权操作。
2. 定义 kernel panic、critical service death、ordinary service fault 三种不同恢复策略。
3. panic dump 使用 CRC、sequence、双槽提交，避免掉电写坏。
4. trace 使用 per-CPU ring 和统一时间戳。
5. 建立 latency histogram：IRQ、调度、IPC call、timer wakeup。
6. 为 syscall 和锁临界区定义有界性预算。
7. 支持 fixed-priority RR/FIFO，并验证 PI；需要时再增加 priority ceiling。
8. 增加 fault injection：内存不足、cap 满、queue 满、IRQ storm、Flash 写失败、service hang。
9. 自动运行 reset/cold boot/brownout 场景。

验收：

- 24 小时双核 soak 无 invariant、泄漏或非预期 reset。
- 关键 IPC/IRQ 延迟有可重复统计，而不是单一 max 值。
- 任何自动测试失败都保留 ELF、配置、串口、trace 和 crash dump。

## M10：安全收敛与接口冻结（4-6 周）

目标：形成可审计的最小可信计算基。

任务：

1. 生成 syscall/capability 权限矩阵。
2. 对所有 syscall 做 pointer/length/cap/type/rights/state 五层检查审计。
3. 默认拒绝所有未声明 platform resource。
4. 为用户镜像、service manifest 和持久配置增加签名验证。
5. secure boot 必须与实际 boot chain 集成，不能只保留 Kconfig。
6. 编译启用 `-fstack-protector` 可行性评估、FORTIFY 风格包装和 LTO 差异测试。
7. host fuzz：IPC parser、name/path、ELF、cap transfer transaction。
8. 建立威胁模型：恶意用户任务、恶意驱动、故障服务、物理攻击分别讨论。

验收：

- 安全配置中不存在“任何用户可申请任意 MMIO/Flash/fault endpoint”的路径。
- ABI 和 capability matrix 进入版本控制，变更必须评审。
- 负向授权测试数量不少于正向功能测试。

## M11：网络与完整 OS 服务（可选主线，6-10 周）

网络不是成为微内核的前置条件，应在 M0-M10 稳定后进行。

任务：

1. CYW43 bus driver 用户态服务。
2. Wi-Fi firmware 由 FS/block server 提供。
3. lwIP 运行在独立 net server，不进入内核。
4. 应用通过 socket IPC facade 使用网络。
5. DHCP、DNS、TLS 分层为可裁剪用户库/服务。
6. 网络服务 fault 不影响 FS、shell 和内核。

## M12：发布工程化（4-6 周）

任务：

1. 固化 stable syscall ABI 1.0。
2. 输出 SDK：用户头、linker template、service protocol、示例应用。
3. RP2350 为主支持，STM32 明确支持等级。
4. release checklist 包含静态分析、构建矩阵、真机矩阵、soak、fault injection 和尺寸预算。
5. 生成 SBOM、第三方许可证和版本 manifest。
6. 文档区分“implemented / experimental / planned”。

---

## 8. 推荐实施顺序与依赖

```text
M0 可信基线
 └─ M1 SMP 正确性
     └─ M2 对象/CSpace
         └─ M3 syscall/IPC ABI
             ├─ M4 地址域/用户 runtime
             │   └─ M5 root/init
             │       └─ M6 服务框架
             │           ├─ M7 用户态驱动
             │           │   └─ M8 用户态持久 FS
             │           └─ M9 监督与诊断
             └─ M10 安全冻结
                 ├─ M11 网络（可选）
                 └─ M12 发布
```

不能颠倒的关键依赖：

- 不应在 M1 前继续扩大 SMP 功能面。
- 不应在 M2 前继续增加大量 cap object 类型。
- 不应在 M3 前继续复制新的阻塞 syscall 状态机。
- 不应在 M5 前把更多服务启动逻辑塞进特权 shell。
- 不应在 M7 前宣称驱动已经真正用户态化。
- 不应在 M8 前把 LittleFS 配置标成可用。
- 不应在 M10 前把 Secure Boot/OTA 标成完成。

---

## 9. 测试战略

### 9.1 测试金字塔

1. Host unit：纯算法、协议解析、cap transaction、路径和 ELF 校验。
2. Kernel model：状态机和不变量随机测试，可在 host 模拟对象池。
3. QEMU/仿真：若架构支持，跑 syscall/IPC 基础回归。
4. RP2350 板上：MPU、fault、SMP、IRQ、Flash、reset。
5. STM32 板上或 compile-only：验证 PMSAv7 和单核回归。
6. 长稳/故障注入：独立于功能测试总数。

### 9.2 每阶段强制门禁

- `-Wall -Wextra -Werror` 构建通过。
- test/dev/release profile 均构建。
- 资源快照 cap/heap/task/endpoint 无非预期变化。
- 单核全回归必须先绿。
- SMP 变更必须跑跨核压力，不允许只跑顺序单元测试。
- 任何权限新增必须同时增加至少一个拒绝测试。
- 任何对象删除路径必须覆盖：正常删除、owner death、waiter death、并发 revoke、id reuse。
- 任何持久化变更必须跑掉电点注入。

### 9.3 自动真机测试输出

每次结果至少保存：

- Git commit。
- `.config` hash 和生成头 hash。
- ELF SHA-256、size、map 文件。
- OpenOCD flash/verify 日志。
- 完整 UART 原始日志。
- 测试模块 pass/fail 和资源快照。
- reset 次数、cold boot 次数、运行时长。
- crash dump 和最近 trace。

---

## 10. 代码组织目标

建议最终目录：

```text
src/
├── arch/                 # Cortex-M33/M7 异常、上下文和原子原语
├── board/                # 只描述平台资源，不承载服务策略
├── kernel/
│   ├── object/           # kobject 生命周期
│   ├── cap/              # CSpace/CNode
│   ├── sched/            # per-CPU scheduler
│   ├── ipc/              # endpoint/reply/notification
│   ├── mm/               # MPU domain/frame/mapping
│   ├── irq/              # IRQ control/ack/notification
│   ├── time/             # monotonic clock/timeout
│   ├── fault/            # fault capture/delivery
│   └── boot/             # root bootstrap
├── user/
│   ├── init/
│   ├── supervisor/
│   ├── nameserver/
│   ├── resource/
│   ├── drivers/
│   ├── fs/
│   ├── shell/
│   ├── log/
│   └── net/
├── userlib/              # syscall/IPC/libc/runtime
└── tests/
    ├── host/
    ├── kernel/
    ├── user/
    └── board/
```

边界规则：

- `src/user` 只能 include `include/uapi` 和 userlib。
- `src/kernel` 不 include 用户服务协议。
- board 只提供资源描述和最早期硬件初始化。
- driver server 不直接链接内核对象 API。
- shell 不直接读取 TCB、cap pool 或 endpoint pool，通过诊断服务快照查询。

---

## 11. 近期 30 天执行清单

### 第 1 周：可信构建

- [ ] 统一唯一配置头。
- [ ] 修复 Make/CMake 配置依赖。
- [ ] 建立 test/dev/release profile。
- [ ] 关闭 release 默认 SMP，保留 smp-test profile。
- [ ] 固化当前板上回归证据。

### 第 2 周：SMP 设计冻结

- [ ] 写 `SMP_INVARIANTS.md`。
- [ ] 决定 per-CPU runqueue 和 IPI 协议。
- [ ] 决定 Core 0 timekeeper。
- [ ] 列出所有只关本核中断的共享结构。
- [ ] 加入任务双核运行检测和 queue ownership 断言。

### 第 3 周：安全阻断项

- [ ] 禁止普通用户调用任意 `sys_mmio_request`。
- [ ] `sys_flash_op` 增加 block capability。
- [ ] fault subscribe 改为 supervisor 专属授权。
- [ ] 增加越权负向测试。
- [ ] task cap 增加临时 target generation 校验，作为 M2 前的止血措施。

### 第 4 周：产品启动骨架

- [ ] release `main` 启动 root/init。
- [ ] test runner 迁入 test profile。
- [ ] shell 用户态化设计和 protocol 定稿。
- [ ] 建立 service manifest 最小版本。
- [ ] 输出首份 capability grant graph。

30 天完成后应达到：构建可信、单核 release 可启动、危险授权路径封闭、SMP 有明确设计而非继续补丁式修复。

---

## 12. 工期与里程碑

单人全职、保持现有质量要求的现实估算：

| 里程碑 | 覆盖阶段 | 预计累计时间 | 可对外描述 |
| --- | --- | ---: | --- |
| R0 可信原型 | M0 | 2 周 | 可复现的微内核 RTOS 原型 |
| R1 单核安全核心 | M0-M4 | 3-5 个月 | MPU 型 capability microkernel core |
| R2 微内核系统 | M0-M7 | 6-9 个月 | 驱动和系统编排在用户态的微内核 OS |
| R3 可恢复完整系统 | M0-M10 | 9-12 个月 | 可长期运行、可审计的嵌入式微内核 OS |
| R4 网络/发布版 | M0-M12 | 12-15 个月 | 带网络和 SDK 的发布级实验系统 |

该估算不包含复杂 Wi-Fi 认证、商业安全认证、完整 POSIX、MMU 进程模型或形式化证明。

---

## 13. 明确不做或延后

为避免项目再次被功能数量拖散，以下内容在微内核边界完成前延后：

- GUI/framebuffer。
- 动态链接和 `dlopen`。
- OTA。
- TLS。
- 完整 POSIX。
- 多用户权限模型。
- 形式化验证宣传。
- 新板卡大规模移植。

Secure Boot 可以做设计准备，但只有 boot chain 真正验签后才能标记完成。

---

## 14. 最终验收场景

项目完成微内核化后，必须现场通过以下场景：

1. 冷启动后 root/init 按 manifest 拉起全部服务，内核没有特权 shell。
2. 普通应用尝试访问 UART MMIO，触发自身 MemManage fault，系统继续运行。
3. UART driver 合法持 cap，可处理 IRQ；driver crash 后 IRQ 被 mask，supervisor 重启后恢复。
4. FS server crash 后重启，持久文件保留，旧 fd 失效但其他服务不崩溃。
5. 恶意任务枚举 cap handle、传裸 task id、伪造 endpoint id 均不能越权。
6. task id/object slot 重复使用后旧 cap 仍然无效。
7. 双核压力下同一任务从不同时运行于两个核。
8. endpoint 客户端 timeout 后，server 的 reply cap 立即失效。
9. cap transfer 在目标 CSpace 满时完整回滚。
10. 连续 100 次 reset、100 次冷上电无启动卡死。
11. 24 小时压力测试无 cap、heap、task、mapping 泄漏。
12. release 镜像关闭 test/shell/trace 后仍从同一内核和同一配置系统构建。

---

## 15. 当前最重要的下一步

不要先增加新驱动、网络或文件系统功能。立即执行 M0，并在 M0 完成后先处理 M1 和三个安全阻断项：

1. 统一配置生成路径。
2. 暂时把 release 恢复为单核，把 SMP 放入专用压力 profile。
3. 设计 per-CPU runqueue，而不是继续让 PendSV 无锁修改共享 ready list。
4. 禁止普通用户任意获取 MMIO、Flash 和 fault endpoint 权限。
5. 把正常启动入口从 test runner 切换为 root/init。

这五项完成后，项目才具备稳定推进后续微内核化的地基。

